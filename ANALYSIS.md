# 坦克大战 - BUG分析与修复清单

> 所有问题已逐行验证源码确认。

---

## P0 · 严重BUG（5个）

### BUG-1 散射道具绕过子弹上限
- **文件**：`js/tank.js:180-184`（上限检查）+ `js/tank.js:101-111`（散射发射）
- **现状**：
  ```
  update()中：if(mine<cap) this.fire();  // 检查时mine=0, cap=2, 通过
  fire()中：散射一次push 3个Bullet       // 结束后mine=3, 超过cap=2
  ```
- **结果**：散射必定超上限；配合连发(cap=4)可一次射6发
- **修复**：散射时将cap视为1（一次散射算一次射击），或fire后立即检查

### BUG-2 HP扣减逻辑错误
- **文件**：`js/tank.js:134-135`
- **现状**：
  ```
  const real = Math.min(dmg, this.hp);  // real = min(55, 30) = 30
  this.hp -= dmg;                       // hp = 30 - 55 = -25（下一行才钳位到0）
  ```
- **结果**：HP可以变成负数再被修正；伤害数字显示原始dmg而非实际伤害
- **修复**：改为 `this.hp -= real;`

### BUG-3 巨型道具血条溢出
- **文件**：`js/tank.js:225` + `js/powerup.js` applyPower mega分支
- **现状**：巨型给+50HP（最高150），血条用 `hp/HP_MAX(100)` 计算
  - 坦克上血条：`bw*1.5 = 44*1.5 = 66px`，背景只有44px → 溢出22px
  - HUD血条：`(w-4)*1.5` 同理溢出
- **修复**：frac钳位到 `Math.min(1, hp/HP_MAX)`，或巨型时同步增大HP_MAX

### BUG-4 友军伤害规则不一致
- **文件**：`js/bullet.js:55` + `js/powerup.js:144-145`
- **子弹友伤**：仅协作模式禁用「玩家→玩家」，AI之间从不禁用
- **地雷友伤**：
  - `gameMode===0`：AI地雷可炸其他AI（无保护）
  - `gameMode===1`：玩家地雷不炸玩家（正确）
  - `gameMode===2`：玩家地雷可炸队友（无保护）
- **修复**：统一规则——同阵营不造成友伤

### BUG-5 协作模式HUD文字遮挡血条
- **文件**：`js/game.js:207-215` + `js/game.js:241-242`
- **现状**：
  ```
  P2血条：x=562, w=320, 右端=882, y=[28..44]
  回合信息：x=CX=800, y=30  → 落在P2血条区域内
  ```
- **修复**：将回合信息移到HUD区域上方或下方，不与血条重叠

---

## P1 · 中等BUG（4个）

### BUG-6 操作说明面板背景不够高
- **文件**：`js/game.js:450-451`（ph=64）+ `js/game.js:473-484`（绘制逻辑）
- **现状**：
  ```
  面板背景高64px
  标题：y+28
  第1行：y+52
  第2行：y+78  → 超出背景14px
  第3行：y+104 → 超出背景40px（仅单人模式）
  ```
- **修复**：根据rows.length动态计算ph

### BUG-7 地图预览缩略图溢出
- **文件**：`js/game.js` drawMenu 地图预览部分
- **现状**：`sc = tw/176 = 0.795`，格子高=0.795×8=6.36px，13行=82.7px，但背景高仅76px
- **修复**：`sc = Math.min(tw/(COLS*8), th/(ROWS*8))`

### BUG-8 伤害数字显示原始值而非实际值
- **文件**：`js/tank.js:139`
- **现状**：`game.parts.text(..., '-'+dmg, ...)` 显示55而非实际扣掉的30
- **修复**：改用 `real` 变量（需先修BUG-2）

### BUG-9 AI躲避散射子弹方向错误
- **文件**：`js/ai.js:75`
- **现状**：`b.dirKey` 对散射子弹是合成值（left/right），不代表真实飞行方向。AI会判断为「横向子弹→上下躲」，但散射子弹实际是斜向的
- **修复**：用 `Math.atan2(b.dir.y, b.dir.x)` 判断真实方向

---

## P2 · 轻微问题（7个）

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 10 | AI共享出生点 | `game.js:51-53,63-65` | 所有AI在同一点出生，第一帧重叠 |
| 11 | AI盯死死亡目标 | `ai.js:28` | 玩家死后AI仍朝尸体移动 |
| 12 | 简单AI太弱 | `ai.js:37-38` | 40%概率追踪，决策间隔1.6-2.6s |
| 13 | 冰冻无数量衰减 | `powerup.js` freeze | 10个AI全冻2.2s，相当于必杀 |
| 14 | EMP过于弱势 | `powerup.js` emp | 无护盾时完全浪费 |
| 15 | HTML canvas尺寸死代码 | `index.html:10` | 900×620被main.js覆盖 |
| 16 | matchStats只增不减 | `game.js:47,56` | 切换模式后残留旧数据 |

---

## 修复计划

### 第一批（P0，5个BUG）
改动小、影响大，优先修复。

| BUG | 改动文件 | 改动量 |
|-----|---------|--------|
| 1 散射上限 | `tank.js` fire()+update() | ~5行 |
| 2 HP扣减 | `tank.js` takeDamage() | 1行 |
| 3 巨型血条 | `tank.js` draw() + `powerup.js` | ~3行 |
| 4 友军伤害 | `bullet.js` + `powerup.js` | ~6行 |
| 5 HUD重叠 | `game.js` drawHUD() | ~2行 |

### 第二批（P1，4个BUG）
UI修正和AI行为修正。

| BUG | 改动文件 | 改动量 |
|-----|---------|--------|
| 6 面板溢出 | `game.js` drawControlPanel() | ~3行 |
| 7 地图溢出 | `game.js` drawMenu() | 1行 |
| 8 伤害数字 | `tank.js` takeDamage() | 1行 |
| 9 AI躲散射 | `ai.js` dodgeBullets() | ~3行 |

### 第三批（P2，7个优化）
游戏性改善，视情况决定。

---

## 当前代码统计

| 文件 | 行数 | 职责 |
|------|------|------|
| utils.js | 40 | 常量、工具函数 |
| audio.js | 66 | 音效（Web Audio API） |
| input.js | ~40 | 键盘输入管理 |
| maps.js | 189 | 12张地图定义+buildMap |
| world.js | ~90 | 地形渲染 |
| particles.js | 147 | 粒子特效（10种类型） |
| powerup.js | 249 | 道具系统（11种+地雷+applyPower） |
| bullet.js | 92 | 子弹逻辑 |
| ai.js | 87 | AI控制器（LOS/路径/躲避） |
| tank.js | 229 | 坦克逻辑（移动/开火/受伤/绘制） |
| game.js | 485 | 游戏主循环+所有UI绘制 |
| main.js | 12 | 启动入口 |
| **合计** | **~1700** | |
