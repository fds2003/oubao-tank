# 坦克大战 - 全面分析报告

## 项目概况
- 技术栈：纯HTML5/Canvas/JS，无依赖，file://兼容
- 画布：1600×900（16:9），场地：22×13格，格子55px，场地1210×715
- 3种游戏模式，12张地图，11种道具，AI系统（3难度，1-10个）

---

## 一、严重BUG（必须修复）

### 1. 散射道具绕过子弹上限
- **文件**：`js/tank.js` fire() + update()
- **问题**：散射一次射3发子弹，但子弹上限检查在fire()之前。MAX_BULLETS=2时，散射一次射出3发，直接超上限。配合连发（上限4）可一次射6发。
- **影响**：散射是最强道具，严重失衡

### 2. HP扣减逻辑错误
- **文件**：`js/tank.js` takeDamage()
- **问题**：`const real=Math.min(dmg,this.hp)` 计算了实际上限，但下面 `this.hp-=dmg` 没用real。HP=30被打55，hp变成-25再被钳位到0。
- **影响**：伤害数字显示不准，逻辑不严谨

### 3. 巨型道具血条溢出
- **文件**：`js/tank.js` draw() + `js/powerup.js` applyPower()
- **问题**：巨型让HP上限到150，但血条用 `hp/HP_MAX(100)` 计算，frac=1.5时血条宽度超背景22像素。
- **影响**：血条视觉溢出

### 4. 友军伤害不一致
- **文件**：`js/bullet.js` + `js/powerup.js`
- **问题**：
  - 子弹：仅协作模式禁用玩家间友伤，AI之间从不禁用
  - 地雷：协作模式玩家地雷可炸队友；单人模式AI地雷可炸其他AI
- **影响**：游戏规则不一致

### 5. 协作模式HUD重叠
- **文件**：`js/game.js` drawHUD()
- **问题**：协作模式P2血条右端到x=882，回合信息居中在x=800，文字叠在P2血条上。
- **影响**：视觉遮挡

---

## 二、中等BUG（应该修复）

### 6. 操作说明面板内容溢出
- **文件**：`js/game.js` drawControlPanel()
- **问题**：面板背景高64px，但3行内容需要约104px，文字溢出背景。
- **影响**：文字没有背景框

### 7. 地图预览网格溢出
- **文件**：`js/game.js` drawMenu() 地图预览
- **问题**：缩略图高76px，但网格实际渲染高度82.7px，溢出6.7px。
- **影响**：地图缩略图边框被内容撑破

### 8. 伤害数字显示原始值
- **文件**：`js/tank.js` takeDamage()
- **问题**：显示 `-55` 而不是实际扣掉的 `-30`。
- **影响**：玩家看到的伤害不准确

### 9. AI躲避散射子弹方向错误
- **文件**：`js/ai.js` dodge()
- **问题**：散射子弹的dirKey是合成值（left/right），不是真实方向。AI会朝错误方向躲避。
- **影响**：AI在散射面前不会躲

---

## 三、轻微问题（可选修复）

### 10. 所有AI共享出生点
- **文件**：`js/game.js` startRound()
- **问题**：所有AI在同一位置出生，靠separateTanks慢慢推开，第一帧重叠。
- **影响**：开局视觉抖动

### 11. AI死亡后仍锁定目标
- **文件**：`js/ai.js` getTarget()
- **问题**：玩家死后AI仍朝尸体位置移动。
- **影响**：AI行为不智能

### 12. 简单AI过于被动
- **文件**：`js/ai.js` getCommand()
- **问题**：简单AI仅40%概率转向玩家方向，决策间隔1.6-2.6秒。
- **影响**：简单难度太简单

### 13. 冰冻道具随AI数量无衰减
- **文件**：`js/powerup.js` applyPower() freeze分支
- **问题**：单人模式10个AI全部冻结2.2秒，相当于必杀技。
- **影响**：AI越多冰冻越强

### 14. EMP道具过于弱势
- **文件**：`js/powerup.js` applyPower() emp分支
- **问题**：只消除护盾，没有护盾时完全浪费。对比其他道具差距大。
- **影响**：EMP是最弱道具

### 15. HTML canvas初始尺寸过时
- **文件**：`index.html`
- **问题**：`width="900" height="620"` 被main.js立即覆盖，是死代码。
- **影响**：代码误导

### 16. matchStats数组只增不减
- **文件**：`js/game.js` startRound()
- **问题**：数组只push不pop，切换模式后残留旧数据。
- **影响**：内存微小浪费

---

## 四、优化建议

### 性能优化
- `particles.js`：每帧创建新数组（filter），可用swap-and-pop避免GC
- `particles.js`：`Math.pow(0.02,dt)` 可简化为固定衰减
- `world.js`：drawGrass遍历全部286格找草地，可预计算草地位置列表

### 游戏性优化
- 增加AI出生点分散（不全挤在一个点）
- 增加简单AI的玩家追踪能力
- 冰冻持续时间随AI数量递减
- EMP增加次要效果（如短暂致盲或减速）
- 巨型道具增加碰撞体积或伤害加成，让"大"有实际意义

---

## 修复优先级

| 优先级 | 编号 | 问题 | 预计改动量 |
|--------|------|------|-----------|
| P0 | 1 | 散射子弹上限 | 小 |
| P0 | 2 | HP扣减逻辑 | 小 |
| P0 | 3 | 巨型血条溢出 | 小 |
| P0 | 4 | 友军伤害 | 中 |
| P1 | 5 | 协作HUD重叠 | 小 |
| P1 | 6 | 操作面板溢出 | 小 |
| P1 | 7 | 地图预览溢出 | 小 |
| P1 | 8 | 伤害数字 | 小 |
| P1 | 9 | AI躲避散射 | 中 |
| P2 | 10-16 | 轻微问题 | 各小 |
